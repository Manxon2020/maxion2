# Microsoft Paint

A small browser-based Microsoft Paint-style drawing app.

Open `index.html` in a browser to use it. It includes pencil, eraser, fill, text, line, rectangle, ellipse, color swatches, brush size, undo/redo, clear, and PNG export.
